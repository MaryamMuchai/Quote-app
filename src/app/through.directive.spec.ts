import { ThroughDirective } from './through.directive';

describe('ThroughDirective', () => {
  it('should create an instance', () => {
    const directive = new ThroughDirective();
    expect(directive).toBeTruthy();
  });
});
